{
  "private": true,
  "scripts": {
    "build": "dotenv -e .env.development -- turbo build",
    "dev": "dotenv -e .env -- turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "@repo/eslint-config": "*",
    "prettier": "^3.1.1",
    "turbo": "latest",
    "dotenv-cli": "latest"
  },
  "packageManager": "yarn@1.22.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
